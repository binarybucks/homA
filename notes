install rvm
curl -L https://get.rvm.io | bash -s stable

put into .zshenv
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

install ruby 1.9.3
rvm install 1.9.2


use ruby 1.9.3
rvm use ruby-1.9.3-p194@homer

enter homer directory
bundle install
